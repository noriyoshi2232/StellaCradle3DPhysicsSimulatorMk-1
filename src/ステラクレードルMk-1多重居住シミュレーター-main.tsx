import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import App from './ステラクレードルMk-1多重居住シミュレーター“StellaCradle is a registered trademark of StellaCradle Corporation.”.tsx';
import './index.css';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>
);
